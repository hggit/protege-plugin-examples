/**
 * 
 */
package com.similar2.matcher.ontology.model;

/**
 * Type used to tag classes during the classification process.
 * Before each classification begins, each class must be tagged
 * with the tag NO_STATUS
 * 
 * @author Philippe Genoud (Philippe.Genoud@imag.fr)
 */
public enum ClassificationStatus {

	NO_STATUS, // the node has not yet been traversed for classification
	IS_EQUIVALENT, // the node is equivalent to the one to classify
	IS_SUPERCLASS, // the node is a super class of the one to classify
	IS_SUBCLASS, // the node is a sub class of the one to classify
	HAS_NO_RELATION, // the node has no inheritance relation with the one to classify
}


